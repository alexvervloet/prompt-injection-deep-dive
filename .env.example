# Copy this file to `.env` and fill in the key for the stack you choose.
#
#   cp .env.example .env
#
# NEVER commit your real .env file. It is already in .gitignore.

# ---------------------------------------------------------------------------
# Guardrails are provider-agnostic — pick whichever you already have set up.
#
#   openai  -> OpenAI chat       (needs OPENAI_API_KEY)
#   claude  -> Claude messages   (needs ANTHROPIC_API_KEY)
#
# Default is "openai".
# ---------------------------------------------------------------------------
PROVIDER=openai

# --- For PROVIDER=openai ---
# https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-key-here

# --- For PROVIDER=claude ---
# https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-your-key-here
